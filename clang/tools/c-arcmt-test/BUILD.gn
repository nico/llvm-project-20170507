executable("c-arcmt-test") {
  configs += [ "//gn:clang_code" ]
  deps = [
    "//clang/tools/libclang",
  ]
  sources = [
    "c-arcmt-test.c",
  ]
  if (host_os == "mac") {
    ldflags = [ "-Wl,-rpath,@loader_path/../lib" ]
  }
}
